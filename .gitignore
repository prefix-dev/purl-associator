# Python / pixi
.pixi/
__pycache__/
*.pyc

# Node
node_modules/
web/dist/
web/public/mappings.json

# Removed CVE/SBOM outputs are owned by downstream projects.

# OSV download cache (legacy/local only; CVE matching is downstream).
.osv_cache/
osv_cache/

# NVD feed cache (used by CPE discovery).
.nvd_cache/
nvd_cache/

# Local scratch outputs
.tmp/

# Editor / OS
.DS_Store
.vscode/
.idea/

# playwright-cli session artifacts (snapshots, console logs, config)
.playwright/
.playwright-cli/

# Wrangler
worker/.wrangler/
worker/dist/

# Local-only context (the user dropped pixi-browse here as a reference, but
# it's an embedded clone — don't push it).
references/
