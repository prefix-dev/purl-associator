# Python / pixi
.pixi/
__pycache__/
*.pyc

# Node
node_modules/
web/dist/
web/public/mappings.json
web/public/cves.json
web/public/sboms.json
web/public/sbom_cves/

# OSV download cache (refreshed daily by cve_refresh workflow).
.osv_cache/
osv_cache/

# NVD feed cache (refreshed daily by cve_refresh workflow).
.nvd_cache/
nvd_cache/

# Local scratch outputs
.tmp/

# Editor / OS
.DS_Store
.vscode/
.idea/

# playwright-cli session artifacts (snapshots, console logs, config)
.playwright/
.playwright-cli/

# Wrangler
worker/.wrangler/
worker/dist/

# Local-only context (the user dropped pixi-browse here as a reference, but
# it's an embedded clone — don't push it).
references/
